//Configuración de la base de datos para el hosting

const config = {
    HOST: "localhost",
    USER: "sergio",
    PASSWORD: "sergio",
    DB: "api",
    dialect: "postgres",
}

module.exports = config;